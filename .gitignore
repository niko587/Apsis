# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
test-results
playwright-report
blob-report
.playwright

# Secrets — server-side only, never committed, never VITE_-prefixed.
.env.local
.env*.local

# Apsis Autopilot (tools/autopilot) — local developer tooling.
# Run records, task worktrees and the local env file. Run records are redacted
# before they are written; this is the second line of defence, not the first.
.apsis-autopilot
.env.autopilot
tools/autopilot/.env.local
.autopilot-goal
